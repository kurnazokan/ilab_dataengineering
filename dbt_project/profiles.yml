ecommerce_analytics:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres
      user: airflow
      password: airflow
      port: 5432
      dbname: ecommerce
      schema: public
      threads: 4
      keepalives_idle: 0

